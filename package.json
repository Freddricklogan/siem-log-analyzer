{
  "name": "siem-log-analyzer",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-based SIEM simulation: normalize, detect, map to MITRE ATT&CK and correlate synthetic security telemetry.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "lint": "eslint . && node src/contrast-check.mjs src/exec-shell.css",
    "validate": "html-validate index.html",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "@vitest/coverage-v8": "^3.2.4",
    "eslint": "^9.37.0",
    "globals": "^16.4.0",
    "html-validate": "^9.7.1",
    "vitest": "^3.2.4"
  }
}
